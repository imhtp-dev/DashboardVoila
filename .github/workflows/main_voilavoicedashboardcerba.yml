# Deploy Next.js Frontend to Azure Web App
# App Service: voilavoicedashboardcerba
# Runtime: Node.js 22 LTS

name: Build and deploy Next.js to Azure Web App - voilavoicedashboardcerba

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  NODE_VERSION: '22.x'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Remove local .env files to prevent conflicts
        run: rm -f .env .env.local .env.development.local

      - name: Build Next.js application
        run: npm run build
        env:
          # Supabase Configuration (REQUIRED)
          NEXT_PUBLIC_SUPABASE_URL: https://yxqconubtqdskcxiobsp.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inl4cWNvbnVidHFkc2tjeGlvYnNwIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjMzNjMzNzYsImV4cCI6MjA3ODkzOTM3Nn0.-JnEq6PTC-xvYL6ePSVcCdQoYMU6gIlgsR6z5fSI63Y

      - name: Prepare standalone deployment
        run: |
          # Copy standalone server (already has its own node_modules)
          cp -r .next/standalone ./deploy
          # Copy static files to the correct location
          mkdir -p ./deploy/.next
          cp -r .next/static ./deploy/.next/static
          # Copy public folder
          cp -r public ./deploy/public
      - name: Upload artifact for deployment
        uses: actions/upload-artifact@v4
        with:
          name: nextjs-app
          path: deploy

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: nextjs-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: 'voilavoicedashboardcerba'
          slot-name: 'Production'

      - name: Verify deployment
        run: |
          echo "✅ Deployment complete with Supabase integration!"
          echo "🌐 Dashboard URL: https://voilavoicedashboardcerba-dzdch8bhcddzdjak.francecentral-01.azurewebsites.net/login"
