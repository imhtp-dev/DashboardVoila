# Deploy Next.js Frontend to Azure Web App
# App Service: voilavoicedashboardcerba
# Runtime: Node.js 22 LTS

name: Build and deploy Next.js to Azure Web App - voilavoicedashboardcerba

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  NODE_VERSION: '22.x'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js application
        run: npm run build
        env:
          # Supabase Configuration (REQUIRED)
          NEXT_PUBLIC_SUPABASE_URL: https://yxqconubtqdskcxiobsp.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inl4cWNvbnVidHFkc2tjeGlvYnNwIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjMzNjMzNzYsImV4cCI6MjA3ODkzOTM3Nn0.-JnEq6PTC-xvYL6ePSVcCdQoYMU6gIlgsR6z5fSI63Y

      - name: Prepare standalone deployment
        run: |
          # Copy the standalone server
          cp -r .next/standalone ./deploy
          # Copy the entire .next folder (not just static) for build metadata
          mkdir -p ./deploy/.next
          cp -r .next/static ./deploy/.next/static
          cp -r .next/server ./deploy/.next/server
          cp .next/build-id ./deploy/.next/build-id || true
          cp .next/package.json ./deploy/.next/package.json || true
          # Copy public folder
          cp -r public ./deploy/public

      - name: Upload artifact for deployment
        uses: actions/upload-artifact@v4
        with:
          name: nextjs-app
          path: deploy

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: nextjs-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_15FFD3170FD2483CB61BAB8949FBF916 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_FD9A980E99EE47E6B6214326D644841A }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_15EA8C0E8E0848A18C8706FB31C4CA4D }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: 'voilavoicedashboardcerba1'
          slot-name: 'Production'

      - name: Verify deployment
        run: |
          echo "✅Deployment complete!"
          echo "🌐 Dashboard URL: voilavoicedashboardcerba1-gaafdfhpawcwa5ek.francecentral-01.azurewebsites.net/login"